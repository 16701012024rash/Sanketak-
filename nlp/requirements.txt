annotated-types==0.7.0
exceptiongroup==1.3.1
iniconfig==2.1.0
packaging==26.3
pluggy==1.6.0
pydantic==2.13.5
pydantic_core==2.46.5
Pygments==2.21.0
pytest==8.4.2
PyYAML==6.0.3
tomli==2.4.1
typing-inspection==0.4.2
typing_extensions==4.16.0
pandas==2.3.3

# LLM providers. Both are imported by src/extraction/llm.py, which picks one
# via LLM_PROVIDER in nlp/.env. Neither was declared before, so a fresh clone
# installed cleanly and then failed at the first extraction.
#
# NOTE: google-generativeai is end-of-life — it emits "All support for the
# `google.generativeai` package has ended. It will no longer be receiving
# updates or bug fixes" on import, and the replacement is the `google-genai`
# package with a different API surface. Pinned here so the current code keeps
# working; migrating is a separate task, not a hotfix.
google-generativeai==0.8.6
groq==1.0.0
